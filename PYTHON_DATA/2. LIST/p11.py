# #?                                LIST FUNCTION
# !     Topic:- count()

#? The count() method is used to count the occurrences of a specified element in a list.

# ! SYNTAX :- COUNT = list.count(element)
# ? element: The element for which you want to count occurrences.

numbers = [1, 2, 3, 4, 2, 2, 5]

count_2 = numbers.count(2)

print(count_2)
